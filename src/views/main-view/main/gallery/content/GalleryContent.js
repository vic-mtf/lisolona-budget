import {Box as MuiBox, Modal, Stack, Divider} from '@mui/material';

export default function GalleryContent () {
    return (
        <MuiBox
            display="flex"
            flex={1}
            width="100%"
            overflow="hidden"
        >
cc
        </MuiBox>
    );
}
import {Box as MuiBox, Modal, Stack, Divider, Toolbar } from '@mui/material';

export default function GalleryHeader () {
    return (
        <Toolbar sx={{bgcolor: 'background.paper'}}>

        </Toolbar>
    );
}